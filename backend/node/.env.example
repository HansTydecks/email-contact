# SMTP settings (use your provider or mailbox)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# From/To
FROM_EMAIL=no-reply@tinfo.space
TO_EMAIL=feedback@tinfo.space

# CORS
ALLOWED_ORIGINS=https://<your-gh-user>.github.io,https://<your-domain>

# Port (Render/Railway provides PORT automatically)
# PORT=8787
